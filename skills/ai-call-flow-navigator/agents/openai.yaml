interface:
  display_name: "AI Call Flow Navigator"
  short_description: "显式分析源码执行链并投递到 Android Studio"
  default_prompt: "使用 $ai-call-flow-navigator 从 MainActivity.onCreate 开始分析调用链，并投递到 Android Studio。"

policy:
  allow_implicit_invocation: false
